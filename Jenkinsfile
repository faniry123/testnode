pipeline {
  agent any
  tools {nodejs "Node"}

  stages {
    stage('Check Node and NPM version') {
      steps {
        sh 'node --version'
      }
    }
    // Ajoutez d'autres étapes au besoin
  }
}
