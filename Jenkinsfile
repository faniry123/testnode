pipeline {
  agent any
  tools {nodejs "Node"}

  stages {
    stage('Check Node and NPM versions') {
      steps {
        bat 'npm install'
      }
    }
    // Ajoutez d'autres étapes au besoin
  }
}
